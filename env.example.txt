# ===========================================
# HireClix CRM - Environment Configuration
# ===========================================
# 1. Rename/copy to .env.local
# 2. Use NEXT_PUBLIC_ prefix (not VITE_) for Next.js
# ===========================================

# REQUIRED: Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Either name works (same "anon" / "publishable" key from Dashboard → Settings → API):
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJ...
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
# Server-side only (API routes, Webflow asset picker, jobs sync): never expose in client bundles
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# App URL for auth redirects (confirmation emails, password reset). Set to production URL when deploying.
# NEXT_PUBLIC_APP_URL=https://your-site.webflow.io
# NEXT_PUBLIC_APP_URL=https://your-site.webflow.io/crm  # If using base path

# Webflow Cloud (set by Webflow at deploy time - leave empty for local)
# BASE_URL=/app
# ASSETS_PREFIX=/app
# NEXT_PUBLIC_BASE_URL=/crm  # Same as BASE_URL; e.g. /crm or /app for Webflow. Auto-detected from URL if unset.

# Career site Jobs: Configure in Settings → Career Site (collection ID + API token).
# No env vars needed; stored in organization_settings.

# Cron (for jobs sync every 15 min): Set CRON_SECRET. Sync runs in Supabase Edge Function (avoids 504).
# Also set JOBS_CRON_SECRET in Supabase: supabase secrets set JOBS_CRON_SECRET=<same as CRON_SECRET>
